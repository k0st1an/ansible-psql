---
# -----------------------------------------------------------------------------
# File: defaults/main.yml
# -----------------------------------------------------------------------------
# Server
psql_version           : "9.5"
psql_tcpip_enable      : false
psql_listen_address    : "localhost"  # *, localhost, 127.0.0.1
psql_listen_port       : 5432
psql_encoding          : "UTF-8"
psql_locale            : "en_US.UTF-8"
psql_template          : "template0"
psql_create_users      : True

psql_users:
  k0st1an:
    enable: "{{ psql_create_users }}"
    name: 'k0st1an'
    password: '42'
    db: 'k0st1an'
    priv: 'SUPERUSER'
    ra_type: "hostssl"
    ra_host: "0.0.0.0/0"
    ra_method: "md5"
